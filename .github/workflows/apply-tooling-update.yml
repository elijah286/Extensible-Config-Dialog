# Apply LabVIEW CI update — one-click FULL upgrade target for the dashboard.
#
# The "Update now" button in the What's New dialog (.github/pages/whats-new.html)
# deep-links here. Dispatching this workflow performs a COMPLETE update: it fetches
# the latest tooling from the source repo (.github/labview-ci.yml: source.repo) and
# runs the catalog-driven installer in --update mode, which re-pulls every tooling
# file for this repo's selected activities (bringing new capabilities, not just a
# version bump), rebrands them to this repo, preserves your own config + baked
# dependencies, and bumps the version. It then commits the update STRAIGHT to the
# default branch — no pull request to open, approve, or merge — using only the
# built-in token's `contents: write` (it does NOT rely on the repo allowing Actions
# to create pull requests, the secure default that used to leave a successful update
# with no PR). Only changes to .github/workflows/** need a one-time
# TOOLING_UPDATE_TOKEN (repo + workflow scope) and are otherwise deferred. If the
# default branch is protected so a direct push is refused, it falls back to opening
# a pull request and enabling auto-merge. GitHub's own "Run workflow" gate limits
# this to write-access users.
name: Apply LabVIEW CI update
run-name: Apply LabVIEW CI update - ${{ github.event_name == 'workflow_dispatch' && 'manual run' || github.sha }}

on:
  workflow_dispatch:
    inputs:
      source_ref:
        description: "Source ref to pull (blank = the source repo's default branch = latest)"
        required: false
        default: ''
      enable_new_capabilities:
        description: 'Also re-enable every stable capability not currently selected (including ones you removed). Off by default so your activity selection is preserved across updates.'
        type: boolean
        default: false

concurrency:
  group: apply-tooling-update
  cancel-in-progress: false

permissions:
  contents: write
  pull-requests: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      # Check TOOLING_UPDATE_TOKEN before anything depends on it.
      #
      # `secrets.X || secrets.GITHUB_TOKEN` only falls back when the secret is
      # ABSENT. An expired PAT is still a non-empty string, so it wins the `||`
      # and gets used -- which makes an expired token strictly worse than no
      # token: checkout dies on `could not read Username for 'https://github.com'`
      # (git's message for "credential rejected, and I can't prompt"), naming
      # neither the token nor the secret. PATs expire on a timer, so every client
      # that adds one eventually walks into this.
      #
      # One unauthenticated-cost API call tells us which case we are in, and the
      # token never leaves this step: it is read from env, sent as a header, and
      # only the HTTP status is recorded.
      - name: Check the tooling update token
        id: token
        env:
          TOOLING_UPDATE_TOKEN: ${{ secrets.TOOLING_UPDATE_TOKEN }}
        run: |
          set -uo pipefail
          emit() { echo "usable=$1" >> "$GITHUB_OUTPUT"; echo "state=$2" >> "$GITHUB_OUTPUT"; }

          if [ -z "${TOOLING_UPDATE_TOKEN:-}" ]; then
            echo "No TOOLING_UPDATE_TOKEN secret; using the built-in token."
            echo "Workflow files (.github/workflows/**) will be deferred if this update changes any."
            emit false absent
            exit 0
          fi

          CODE=$(curl -sS -o /dev/null -w '%{http_code}' --max-time 30 \
                   -H "Authorization: Bearer ${TOOLING_UPDATE_TOKEN}" \
                   -H "Accept: application/vnd.github+json" \
                   -H "X-GitHub-Api-Version: 2022-11-28" \
                   "https://api.github.com/repos/${GITHUB_REPOSITORY}" || echo "000")

          case "$CODE" in
            200)
              echo "TOOLING_UPDATE_TOKEN accepted; workflow files can be updated."
              emit true ok ;;
            401)
              echo "::warning title=TOOLING_UPDATE_TOKEN expired::GitHub rejected this repository's TOOLING_UPDATE_TOKEN (HTTP 401). Personal access tokens expire; this one needs rotating. Continuing with the built-in token, so any workflow-file changes in this update will be deferred."
              emit false expired ;;
            403|404)
              echo "::warning title=TOOLING_UPDATE_TOKEN lacks access::This repository's TOOLING_UPDATE_TOKEN authenticated but cannot see ${GITHUB_REPOSITORY} (HTTP ${CODE}) — it is scoped to the wrong repository, or is missing Contents access. Continuing with the built-in token, so any workflow-file changes in this update will be deferred."
              emit false no_access ;;
            000)
              echo "::warning title=Could not verify the token::api.github.com was unreachable, so TOOLING_UPDATE_TOKEN could not be checked. Using the built-in token for this run."
              emit false unreachable ;;
            *)
              echo "::warning title=TOOLING_UPDATE_TOKEN unusable::Checking this repository's TOOLING_UPDATE_TOKEN returned HTTP ${CODE}. Continuing with the built-in token, so any workflow-file changes in this update will be deferred."
              emit false unknown ;;
          esac

      - name: Explain how to rotate the token
        if: steps.token.outputs.usable != 'true' && steps.token.outputs.state != 'absent' && steps.token.outputs.state != 'unreachable'
        env:
          STATE: ${{ steps.token.outputs.state }}
        run: |
          set -euo pipefail
          case "$STATE" in
            expired)
              HEADLINE="Your TOOLING_UPDATE_TOKEN has expired"
              CAUSE="GitHub rejected it (HTTP 401). Personal access tokens expire on a schedule; this one has reached its expiry date." ;;
            no_access)
              HEADLINE="Your TOOLING_UPDATE_TOKEN cannot access this repository"
              CAUSE="It authenticated, but has no Contents access to \`${GITHUB_REPOSITORY}\` — usually a fine-grained token scoped to a different repository." ;;
            *)
              # Don't guess a cause from an unexpected status: a 5xx is GitHub
              # having a bad day, not necessarily anything wrong with the token.
              HEADLINE="Your TOOLING_UPDATE_TOKEN could not be verified"
              CAUSE="Checking it returned an unexpected response from api.github.com. This may be transient — re-running **Update now** is worth trying before replacing the token." ;;
          esac
          {
            echo "## ⚠️ ${HEADLINE}"
            echo ""
            echo "${CAUSE}"
            echo ""
            echo "**This update still ran** using the built-in token, so everything except"
            echo "\`.github/workflows/**\` was applied. Workflow YAMLs keep their current version"
            echo "until the token is replaced — the built-in token is not allowed to push them."
            echo ""
            echo "### Rotate it"
            echo ""
            echo "**From the dashboard (easiest):** open **What's New ▸ Update now**. It offers an"
            echo "\"add a one-time token\" field with a ready-made token link; paste a fresh token"
            echo "there and it stores the secret and restarts the update for you."
            echo ""
            echo "**Or replace the secret yourself:** create a token — fine-grained with"
            echo "**Contents + Workflows = Read and write** on this repository, or a classic PAT with"
            echo "\`repo\` + \`workflow\` — then update **TOOLING_UPDATE_TOKEN** under"
            echo "**Settings ▸ Secrets and variables ▸ Actions** and re-run **Update now**."
            echo ""
            echo "Deleting the secret is also valid: the update then always runs on the built-in"
            echo "token and simply defers workflow files."
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Checkout
        uses: actions/checkout@v4
        with:
          # A PAT with repo + workflow scope (added as TOOLING_UPDATE_TOKEN) lets the
          # update push changes to .github/workflows/** — the default token cannot.
          # Only used once verified above, so a stale PAT degrades to a deferred
          # workflow update instead of failing the run at checkout.
          token: ${{ steps.token.outputs.usable == 'true' && secrets.TOOLING_UPDATE_TOKEN || secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Fetch latest tooling and run the installer in update mode
        id: apply
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          INPUT_REF: ${{ inputs.source_ref }}
          ENABLE_NEW: ${{ inputs.enable_new_capabilities }}
        run: |
          set -euo pipefail
          CFG=.github/labview-ci.yml
          if [ ! -f "$CFG" ]; then
            echo "::error::$CFG not found — this repo is not configured as a LabVIEW CI consumer."
            exit 1
          fi

          SRC_REPO=$(awk '/^[[:space:]]*repo:[[:space:]]/{print $2; exit}' "$CFG")
          SRC_REPO=${SRC_REPO:-elijah286/Extensible-Config-Dialog}
          INSTALLED=$(awk '/^[[:space:]]*installedVersion:[[:space:]]/{print $2; exit}' "$CFG")
          MAXPAR=$(awk '/^[[:space:]]*maxParallel:[[:space:]]/{print $2; exit}' "$CFG")
          echo "source repo: $SRC_REPO  installed: ${INSTALLED:-unknown}"

          # Relocation pointer. The single place that lets the whole tooling move
          # to a new official home (e.g. an NI-owned repo): .github/labview-ci/source.json
          # in the source repo names where the tooling officially lives. If it names a
          # DIFFERENT repo than the one we pull from, the tooling has relocated, so follow
          # it and pull this update from the new home. install.py records the FETCHED
          # catalog's source.repo into .github/labview-ci.yml, so after this one redirected
          # update the client polls the new home directly (no more hops). Probe the source's
          # live channel (its default branch via HEAD); a pinned source_ref is honored as-is.
          # NOTE: keep this python one-liner on a SINGLE line. A multi-line inline
          # `python3 -c '...'` would sit at column 0 and break out of this YAML
          # `run: |` block scalar (invalid-workflow-file error). The surrounding
          # `2>/dev/null || echo ""` already yields "" on empty/invalid JSON, so no
          # in-python try/except is needed.
          SUCC=$(curl -fsSL "https://raw.githubusercontent.com/${SRC_REPO}/HEAD/.github/labview-ci/source.json" 2>/dev/null \
            | python3 -c 'import sys,json; print((json.load(sys.stdin).get("repo") or "").strip())' 2>/dev/null || echo "")
          if [ -n "$SUCC" ] && [ "$(printf %s "$SUCC" | tr A-Z a-z)" != "$(printf %s "$SRC_REPO" | tr A-Z a-z)" ]; then
            echo "::notice::LabVIEW CI tooling has relocated: ${SRC_REPO} -> ${SUCC}. Pulling the update from the new home."
            SRC_REPO="$SUCC"
          fi

          REF="$INPUT_REF"
          if [ -z "$REF" ]; then
            REF=$(gh api "repos/$SRC_REPO" --jq '.default_branch' 2>/dev/null || echo main)
          fi
          echo "fetching ${SRC_REPO}@${REF}"

          SRC_DIR="$(mktemp -d)"
          if ! curl -fsSL "https://codeload.github.com/${SRC_REPO}/tar.gz/${REF}" | tar -xz -C "$SRC_DIR" --strip-components=1; then
            echo "::error::could not download tooling from ${SRC_REPO}@${REF}."
            exit 1
          fi
          if [ ! -f "$SRC_DIR/.github/labview-ci/install.py" ]; then
            echo "::error::installer not found in the fetched tooling."
            exit 1
          fi

          LATEST=$(python3 -c "import json,sys;print(json.load(open(sys.argv[1]))['version'])" "$SRC_DIR/.github/labview-ci/catalog.json")
          echo "latest published version: $LATEST"

          # Activity set: keep current selection, optionally add new stable capabilities.
          ACTS=$(SRC_DIR="$SRC_DIR" python3 - <<'PY'
          import os, re, json
          cur, in_acts = [], False
          for line in open('.github/labview-ci.yml', encoding='utf-8'):
              if re.match(r'^\s*activities:\s*$', line):
                  in_acts = True; continue
              if in_acts:
                  m = re.match(r'^\s*-\s*(\S+)', line)
                  if m: cur.append(m.group(1)); continue
                  in_acts = False
          if os.environ.get('ENABLE_NEW', 'false').lower() == 'true':
              cat = json.load(open(os.path.join(os.environ['SRC_DIR'], '.github/labview-ci/catalog.json'), encoding='utf-8'))
              for c in cat.get('capabilities', []):
                  if c.get('status') == 'stable' and c['id'] not in cur:
                      cur.append(c['id'])
          print(','.join(cur))
          PY
          )
          echo "activities to install: $ACTS"

          python3 "$SRC_DIR/.github/labview-ci/install.py" --update \
            --source "$SRC_DIR" --target "$PWD" \
            --repo "$GITHUB_REPOSITORY" \
            --activities "$ACTS"

          # Restore runner concurrency (install.py's manifest writer omits it).
          if [ -n "${MAXPAR:-}" ]; then
            MAXPAR="$MAXPAR" python3 - <<'PY'
          import os, re
          mp = os.environ['MAXPAR']
          p = '.github/labview-ci.yml'
          lines = open(p, encoding='utf-8').read().splitlines()
          if not any('maxParallel:' in l for l in lines):
              out = []
              for line in lines:
                  out.append(line)
                  if re.match(r'^\s*os:\s*\[', line):
                      out.append('  concurrency:')
                      out.append(f'    maxParallel: {mp}')
              open(p, 'w', encoding='utf-8').write('\n'.join(out) + '\n')
              print(f'restored maxParallel: {mp}')
          PY
          fi

          {
            echo "installed=${INSTALLED:-unknown}"
            echo "latest=$LATEST"
            echo "source_repo=$SRC_REPO"
          } >> "$GITHUB_OUTPUT"

      - name: Apply the update
        env:
          # Same guard as the checkout: a verified token only, so a stale PAT
          # cannot make the push half-fail after the update has been staged.
          GH_TOKEN: ${{ steps.token.outputs.usable == 'true' && secrets.TOOLING_UPDATE_TOKEN || secrets.GITHUB_TOKEN }}
          INSTALLED: ${{ steps.apply.outputs.installed }}
          LATEST: ${{ steps.apply.outputs.latest }}
          SOURCE_REPO: ${{ steps.apply.outputs.source_repo }}
          DEFAULT_BRANCH: ${{ github.ref_name }}
        run: |
          set -euo pipefail
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          if [ -z "$(git status --porcelain)" ]; then
            {
              echo "## ✅ Already up to date"
              echo ""
              echo "This repository already runs the latest LabVIEW CI tooling (**v${LATEST}**). No changes were needed."
            } >> "$GITHUB_STEP_SUMMARY"
            echo "No changes — already up to date."
            exit 0
          fi

          DEFAULT_BRANCH="${DEFAULT_BRANCH:-${GITHUB_REF_NAME:-main}}"

          git add -A
          CHANGED=$(git diff --cached --name-only | wc -l | tr -d ' ')
          git commit -m "ci: adopt LabVIEW CI tooling v${LATEST} (full update)"

          # Strip every .github/workflows/** change out of the last commit so the
          # built-in token (which lacks `workflow` scope) can push the rest. Reverts
          # modified + deleted workflow files AND deletes brand-new ones (a plain
          # `git checkout HEAD -- path` would leave new files behind, so the re-push
          # is refused again). Sets DEFERRED_WF to the count held back.
          DEFERRED_WF=0
          strip_workflows() {
            DEFERRED_WF=$(git diff --name-only HEAD~1 HEAD -- '.github/workflows' | grep -c . || true)
            git reset --soft HEAD~1
            git reset -q HEAD -- '.github/workflows' 2>/dev/null || true
            git checkout -q -- '.github/workflows' 2>/dev/null || true
            git clean -fdq -- '.github/workflows' 2>/dev/null || true
            git add -A
          }

          # ---- Primary path: commit the update STRAIGHT to the default branch — no
          # pull request, no manual merge. A direct push needs only `contents: write`
          # (the built-in token has it) and does NOT depend on the repo's "Allow
          # GitHub Actions to create pull requests" setting — that secure default is
          # exactly what used to leave a successful update with no PR to merge. The
          # dashboard + pages rebuild that the push triggers publishes the new
          # version on its own; only .github/workflows/** still needs a token.
          PUSHED=0
          push_default() { git push origin "HEAD:${DEFAULT_BRANCH}" 2>/tmp/push-err; }

          if push_default; then
            PUSHED=1
          else
            cat /tmp/push-err || true
            # Lost a race to a concurrent push? Rebase onto the new tip and retry once.
            if grep -qiE 'fetch first|non-fast-forward|tip of your current branch is behind' /tmp/push-err; then
              git fetch --no-tags origin "${DEFAULT_BRANCH}" >/dev/null 2>&1 || true
              if git rebase FETCH_HEAD >/dev/null 2>&1; then
                if push_default; then PUSHED=1; else cat /tmp/push-err || true; fi
              else
                git rebase --abort >/dev/null 2>&1 || true
              fi
            fi
            # Refused ONLY because the commit touches .github/workflows/** and the
            # token lacks `workflow` scope (not a branch-protection refusal): defer
            # the YAMLs and push the rest straight to the default branch. The YAMLs
            # are thin runtime callers, so the version still adopts without them.
            if [ "$PUSHED" != 1 ] && grep -qi 'workflow' /tmp/push-err \
               && ! grep -qiE 'protected|pull request|hook declined|rule' /tmp/push-err; then
              strip_workflows
              if [ -n "$(git diff --cached --name-only)" ]; then
                CHANGED=$(git diff --cached --name-only | wc -l | tr -d ' ')
                git commit -m "ci: adopt LabVIEW CI tooling v${LATEST} (config + scripts; ${DEFERRED_WF} workflow file(s) deferred)"
                if push_default; then PUSHED=1; else cat /tmp/push-err || true; fi
              else
                # Everything in this release is workflow files; the built-in token
                # can't push any of it directly. Ask for a one-time token.
                {
                  echo "## ⚠️ This update only changes workflow files — add a one-time token"
                  echo ""
                  echo "Everything new in **v${LATEST}** lives in GitHub **workflow files**, which the built-in"
                  echo "token cannot push. Finish it from the dashboard:"
                  echo ""
                  echo "**From the dashboard, no settings trip.** Open **What's New ▸ Update now**. When the update can't finish on its own it shows an **\"add a one-time token\"** field with a ready-made token link (Contents + Workflows + Pull requests + Secrets = Read and write). Paste the token there; the page seals it in your browser, stores it as this repo's \`TOOLING_UPDATE_TOKEN\` secret, and restarts the update — which then commits the workflow files for you."
                  echo ""
                  echo "**Or add the secret yourself:** create a token (classic PAT \`repo\` + \`workflow\`, or fine-grained **Contents + Workflows = Read and write**), add it as the repository secret **TOOLING_UPDATE_TOKEN** (Settings ▸ Secrets and variables ▸ Actions), then re-run **Update now**."
                } >> "$GITHUB_STEP_SUMMARY"
                echo "::warning title=Token needed::This update only changes workflow files; pushing them needs a one-time TOOLING_UPDATE_TOKEN. Add it inline from the What's New > Update now page, or as a repo secret."
                exit 0
              fi
            fi
          fi

          # ---- Direct push succeeded: the update is applied, no PR. Report it and
          # tidy away any stale update PR/branch left by an older attempt.
          if [ "$PUSHED" = 1 ]; then
            {
              echo "## ✅ Update applied — v${INSTALLED} → v${LATEST}"
              echo ""
              echo "Committed the latest LabVIEW CI tooling straight to \`${DEFAULT_BRANCH}\` (**${CHANGED} file(s)**, no pull request to merge). Your dashboard rebuilds and the version badge turns green within a minute."
              if [ "${DEFERRED_WF:-0}" -gt 0 ]; then
                echo ""
                echo "> ⚠️ **${DEFERRED_WF} workflow file(s) deferred** — the built-in token can't push \`.github/workflows/**\`, so those YAMLs keep their current version. The dashboard and reusable tooling still update (they load the tooling at the bumped ref at runtime). To also refresh the workflow YAMLs, add a **TOOLING_UPDATE_TOKEN** secret (repo + workflow scope) and re-run **Update now**."
              fi
            } >> "$GITHUB_STEP_SUMMARY"
            echo "Applied the update directly to ${DEFAULT_BRANCH}."
            gh pr list --state open --json number,headRefName \
              --jq '.[] | select(.headRefName|test("^ci/(adopt|update)-tooling-v")) | "\(.number)\t\(.headRefName)"' 2>/dev/null \
            | while IFS=$'\t' read -r num ref; do
                [ -z "${num:-}" ] && continue
                gh pr close "$num" --delete-branch --comment "Superseded — the update to **v${LATEST}** was applied directly to \`${DEFAULT_BRANCH}\`, so there is nothing to merge here." >/dev/null 2>&1 || true
              done || true
            exit 0
          fi

          # ---- Fallback: the direct push was refused by branch protection / a
          # ruleset (the default branch requires a pull request). Open one — and turn
          # on auto-merge below — so it still finishes hands-off wherever the repo
          # permits; otherwise it waits for the review the protection asks for.
          echo "::notice::A direct push to ${DEFAULT_BRANCH} was refused; opening a pull request instead."
          BRANCH="ci/adopt-tooling-v${LATEST}"

          {
            echo "Adopts the latest LabVIEW CI tooling (**v${INSTALLED} → v${LATEST}**) from \`${SOURCE_REPO}\`."
            echo ""
            echo "Your default branch requires changes to go through a pull request, so the update is offered here. **${CHANGED} file(s)** changed — every tooling file for this repo's selected capabilities was re-pulled at the latest version (new capabilities included), rebranded to this repo, with your own configuration and baked dependencies preserved."
            echo ""
            echo "Triggered from the dashboard **Update now** button. If the repository permits it, this PR **auto-merges** once its requirements are met; otherwise review and merge to finish — the version badge turns green and the pipeline runs the updated capabilities."
            echo ""
            echo "> **See an older update PR, or a merge conflict?** You can ignore it — only this PR (the newest, **v${LATEST}**) matters. Any older \`ci/adopt-tooling-*\` update PR is closed automatically. And if this PR ever shows a conflict on \`catalog.json\` / \`labview-ci.yml\`, don't hand-resolve it: just click **Update now** again. That rebuilds this branch from the latest default branch, which clears the conflict (the update replaces those files wholesale, so there is never anything real to merge by hand)."
          } > /tmp/pr-body.md

          if ! git push -f origin "HEAD:refs/heads/${BRANCH}" 2>/tmp/push-err; then
            cat /tmp/push-err || true
            if grep -qi 'workflow' /tmp/push-err; then
              strip_workflows
              if [ -z "$(git diff --cached --name-only)" ]; then
                {
                  echo "## ⚠️ This update only changes workflow files — add a one-time token"
                  echo ""
                  echo "Everything new in **v${LATEST}** lives in GitHub **workflow files**, which the built-in token cannot push, and your default branch requires a pull request. Open **What's New ▸ Update now** and paste the one-time token it offers (Contents + Workflows + Pull requests + Secrets = Read and write); it stores the \`TOOLING_UPDATE_TOKEN\` secret and finishes the update."
                } >> "$GITHUB_STEP_SUMMARY"
                echo "::warning title=Token needed::This update only changes workflow files; add a one-time TOOLING_UPDATE_TOKEN from the What's New > Update now page."
                exit 0
              fi
              CHANGED=$(git diff --cached --name-only | wc -l | tr -d ' ')
              git commit -m "ci: adopt LabVIEW CI tooling v${LATEST} (config + scripts; ${DEFERRED_WF} workflow file(s) deferred)"
              if ! git push -f origin "HEAD:refs/heads/${BRANCH}" 2>/tmp/push-err2; then
                cat /tmp/push-err2 || true
                echo "::error title=Push failed::Could not push the update branch."
                exit 1
              fi
            else
              echo "::error title=Push failed::Could not push the update branch."
              exit 1
            fi
          fi

          # When workflow YAMLs were held back, say so in the PR — the version
          # still adopts (runtime callers load the bumped ref), only the YAMLs wait
          # on a PAT.
          if [ "${DEFERRED_WF:-0}" -gt 0 ]; then
            {
              echo ""
              echo "> ⚠️ **${DEFERRED_WF} workflow file(s) deferred.** The token available to this run cannot push"
              echo "> \`.github/workflows/**\`, so those YAMLs keep their current version. The dashboard and reusable"
              echo "> tooling still update — they load the tooling at the bumped ref at runtime. To also refresh the"
              echo "> workflow YAMLs, add a **TOOLING_UPDATE_TOKEN** secret (repo + workflow scope) and re-run **Update now**."
            } >> /tmp/pr-body.md
          fi

          URL=$(gh pr list --head "$BRANCH" --state open --json url --jq '.[0].url // empty')
          if [ -n "$URL" ]; then
            gh pr edit "$URL" --body-file /tmp/pr-body.md >/dev/null || true
            echo "Updated existing PR: $URL"
          elif URL=$(gh pr create --title "ci: adopt LabVIEW CI tooling v${LATEST}" \
                       --base "${{ github.ref_name }}" --head "$BRANCH" \
                       --body-file /tmp/pr-body.md 2>/tmp/pr-err); then
            echo "Opened PR: $URL"
          else
            cat /tmp/pr-err || true
            URL="${{ github.server_url }}/${{ github.repository }}/compare/${{ github.ref_name }}...${BRANCH}?expand=1"
            echo "::warning title=Open the PR manually::GitHub Actions is not permitted to open PRs in this repo. Enable Settings > Actions > General > 'Allow GitHub Actions to create and approve pull requests', or use the link in the summary. The branch is already pushed."
            {
              echo "## ⬆️ Upgrade branch ready — open the pull request"
              echo ""
              echo "Adopts LabVIEW CI tooling **v${INSTALLED} → v${LATEST}** (committed to branch \`${BRANCH}\`, ${CHANGED} file(s))."
              echo ""
              echo "**[Open the pull request](${URL})** to review and merge."
              echo ""
              echo "> To make this fully one-click next time, enable **Settings > Actions > General > Workflow permissions > 'Allow GitHub Actions to create and approve pull requests'**."
            } >> "$GITHUB_STEP_SUMMARY"
            exit 0
          fi

          # Auto-merge where the repo allows it (Allow auto-merge enabled + all
          # requirements met) so even a protected default branch finishes without a
          # manual click; if it isn't permitted this no-ops and the PR waits for the
          # review the branch protection asks for.
          if [ -n "${URL:-}" ]; then
            gh pr merge "$URL" --merge --auto >/dev/null 2>&1 \
              || gh pr merge "$URL" --merge >/dev/null 2>&1 \
              || echo "::notice::Opened the pull request; merge it to finish (auto-merge isn't available here)."
          fi

          # Keep only ONE open update PR. A previous "Update now" that was never
          # merged leaves a per-version branch behind; once this repo's default
          # branch moves on, that stale branch collides on catalog.json /
          # labview-ci.yml — a "conflict" that is never a real conflict, because the
          # update overwrites those files wholesale. Close every OTHER open update
          # PR and delete its branch so the newest one is the only thing you see.
          gh pr list --state open --json number,headRefName \
            --jq '.[] | select(.headRefName|test("^ci/(adopt|update)-tooling-v")) | "\(.number)\t\(.headRefName)"' 2>/dev/null \
          | while IFS=$'\t' read -r num ref; do
              if [ -z "${num:-}" ] || [ "$ref" = "$BRANCH" ]; then continue; fi
              if gh pr close "$num" --delete-branch \
                   --comment "Superseded by the newer update to **v${LATEST}** (branch \`${BRANCH}\`). This PR targeted an older version and would conflict with the current default branch, so it is closed automatically — there is nothing to resolve here." >/dev/null 2>&1; then
                echo "Superseded stale update PR #$num ($ref)"
              else
                echo "Could not close PR #$num ($ref) — continuing"
              fi
            done || true

          {
            echo "## ✅ Upgrade pull request ready"
            echo ""
            echo "Adopts LabVIEW CI tooling **v${INSTALLED} → v${LATEST}** (${CHANGED} file(s) changed)."
            echo ""
            echo "If the repository allows it this PR **auto-merges** once its requirements are met. Otherwise **[review &amp; merge it](${URL})** to finish the update."
          } >> "$GITHUB_STEP_SUMMARY"
